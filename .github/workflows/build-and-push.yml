name: Build & Publish Images

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**/*.md'
      - '**/*.txt'
      - '.gitignore'
      - 'LICENSE'
      - 'README*'
      - 'docs/**'
  workflow_dispatch:

concurrency:
  group: docker-publish-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prepare:
    uses: ./.github/workflows/matrix-generator.yml
    with:
      php_versions: '["8.4"]'
      os_versions: '["trixie"]'
      blueprints: |
        {
          "builder": {
            "build_args": ["NODE=24"],
            "test_command": "docker run --rm \"$IMAGE\" verify-builder.sh"
          },
          "fpm-apache": {},
          "apache": {
            "test_command": "docker rm -f apache-php-test >/dev/null 2>&1 || true; docker run -d --name apache-php-test -p 8080:8080 -e DRUPAL_SUBDIR=/test-site -e PHP_UPLOAD_MAX_FILESIZE=128M -e PHP_POST_MAX_SIZE=129M -e PHP_MAX_EXECUTION_TIME=180 \"$IMAGE\"; cleanup() { docker logs apache-php-test || true; docker rm -f apache-php-test || true; }; trap cleanup EXIT; sleep 8; docker exec apache-php-test verify-apache-php.sh"
          },
          "apache-fpm": {
            "test_command": "docker rm -f apache-php-test >/dev/null 2>&1 || true; docker run -d --name apache-php-test -p 8080:8080 -e DRUPAL_SUBDIR=/test-site -e PHP_UPLOAD_MAX_FILESIZE=128M -e PHP_POST_MAX_SIZE=129M -e PHP_MAX_EXECUTION_TIME=180 \"$IMAGE\"; cleanup() { docker logs apache-php-test || true; docker rm -f apache-php-test || true; }; trap cleanup EXIT; sleep 8; docker exec apache-php-test verify-apache-php.sh"
          }
        }

  build:
    needs: prepare
    permissions:
      contents: read

    strategy:
      max-parallel: 4
      matrix:
        include: ${{ fromJson(needs.prepare.outputs.matrix) }}

    uses: alchatti/.github/.github/workflows/docker-build-push.yml@main
    with:
      context: .
      dockerfile: ${{ matrix.context }}/Dockerfile
      platforms: linux/amd64,linux/arm64
      build_args: ${{ matrix.build_args }}
      tags: ${{ matrix.tags }}
      target: ${{ matrix.target }}

      test_command: ${{ matrix.test_command }}
      test_platform: linux/amd64
      test_image_tag: ${{ matrix.test_image_tag }}

      cache_from: type=gha,scope=${{ matrix.variant }}
      cache_to: type=gha,mode=max,scope=${{ matrix.variant }}

    secrets: inherit
